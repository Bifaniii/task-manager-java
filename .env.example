# Database Configuration
# Use root user for development (change in production!)
DATABASE_USERNAME=root
DATABASE_PASSWORD=sua_senha_forte_aqui

# JWT Configuration
# Generate a strong secret key in production!
# Example: openssl rand -base64 32
JWT_SECRET=sua_chave_secreta_muito_forte_e_unica_aqui
